# Erlang version
erlang_version=20.3

# Elixir version
elixir_version=1.6.4

# Rebar version
rebar_version=(tag 3.5.0)

# Do dependencies have to be built from scratch on every deploy?
always_build_deps=true
